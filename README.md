# AlgoPulse

AlgoPulse is a small, polished algorithm visualizer built with plain HTML, CSS, and JavaScript.

It includes three interactive modes:

- Bubble sort with comparison and swap metrics
- Breadth-first search pathfinding on a randomized grid
- Hash table insertion using separate chaining

## Run locally

Open `index.html` in a browser, or serve the folder:

```bash
python -m http.server 8080
```

Then visit `http://localhost:8080`.

## Why this is GitHub-ready

- No build step
- No dependencies
- Responsive layout
- Real algorithm logic instead of static mockups
- Works well on GitHub Pages
